<!--pages/family/family.wxml-->
<view class="page">
  <!-- ===== 邀请阶段：独立加入流程，不展示创建家庭 ===== -->
  <block wx:if="{{stage === 'inviteLoading' || stage === 'inviteJoin' || stage === 'inviteInvalid' || stage === 'inviteConflict'}}">
    <view class="family-header family-header-invite">
      <view class="family-bg"></view>
      <view class="family-inner family-inner-create">
        <view class="family-icon-wrap">
          <view class="family-icon">🏠</view>
        </view>
        <view class="family-name">邀请你加入家庭药箱</view>
        <view class="family-info">
          <text>加入后可与家人共享药品、库存和提醒</text>
        </view>
      </view>
    </view>

    <view class="container invite-page-container">
      <view wx:if="{{stage === 'inviteLoading'}}" class="invite-state-card">
        <view class="invite-state-icon">⌛</view>
        <view class="invite-state-title">正在验证家庭邀请</view>
        <view class="invite-state-desc">请稍候，我们正在确认邀请码是否有效。</view>
      </view>

      <view wx:elif="{{stage === 'inviteJoin'}}" class="cloud-join-card cloud-join-card-primary">
        <view class="invite-family-label">你将加入</view>
        <view class="invite-family-name">{{invitePreview.familyName}}</view>
        <view class="cloud-join-desc">
          邀请码：<text class="invite-code-big">{{inviteCodeFromLink}}</text>
          <text wx:if="{{invitePreview.expiresText}}" class="invite-expire-text">有效至 {{invitePreview.expiresText}}</text>
        </view>
        <view class="app-form-field">
          <view class="app-form-label"><text class="app-form-required">*</text> 你的姓名或称呼</view>
          <input class="app-form-input" value="{{joinForm.name}}" bindinput="onJoinNameInput" placeholder="例如：爸爸、妈妈" />
        </view>
        <view class="app-form-field">
          <view class="app-form-label">与家庭关系</view>
          <input class="app-form-input" value="{{joinForm.relation}}" bindinput="onJoinRelationInput" placeholder="例如：父亲、母亲、配偶" />
        </view>
        <view class="app-form-field">
          <view class="app-form-label">加入身份</view>
          <view class="app-option-row">
            <view class="app-option-chip {{joinForm.role === 'member' ? 'app-option-chip-active' : ''}}" data-role="member" bindtap="chooseJoinRole">家庭成员</view>
            <view class="app-option-chip {{joinForm.role === 'elder' ? 'app-option-chip-active' : ''}}" data-role="elder" bindtap="chooseJoinRole">老人</view>
          </view>
          <view class="app-form-helper">新成员默认只读，编辑权限由家庭管理员设置。</view>
        </view>
        <view class="btn-create" bindtap="submitJoinByInvite">加入家庭</view>
        <view class="invite-secondary-action" bindtap="cancelInvite">暂不加入</view>
      </view>

      <view wx:elif="{{stage === 'inviteConflict'}}" class="invite-state-card">
        <view class="invite-state-icon">🏠</view>
        <view class="invite-state-title">你已经加入一个家庭</view>
        <view class="invite-state-desc">当前家庭为「{{existingFamilyName}}」，暂不支持同时加入「{{invitePreview.familyName}}」。</view>
        <view class="btn-create" bindtap="viewCurrentFamily">查看当前家庭</view>
        <view class="invite-secondary-action" bindtap="cancelInvite">暂不加入</view>
      </view>

      <view wx:else class="invite-state-card">
        <view class="invite-state-icon">!</view>
        <view class="invite-state-title">无法使用这份邀请</view>
        <view class="invite-state-desc">{{inviteErrorText}}</view>
        <view class="btn-create" bindtap="retryInvitePreview">重新加载</view>
        <view class="invite-secondary-action" bindtap="cancelInvite">返回首页</view>
      </view>

      <view class="tip-box invite-tip-box">
        <view class="tip-icon">💚</view>
        <view class="tip-text">
          <view class="tip-title">药无忧 · 小贴士</view>
          <view class="tip-desc">加入后只共享家庭药品管理数据，不提供医疗诊断和用药建议。</view>
        </view>
      </view>
    </view>
  </block>

  <!-- ===== 无家庭阶段：创建家庭表单 ===== -->
  <block wx:elif="{{stage === 'noFamily'}}">
    <view class="family-header">
      <view class="family-bg"></view>
      <view class="family-inner family-inner-create">
        <view class="family-icon-wrap">
          <view class="family-icon">🏠</view>
        </view>
        <view class="family-name">创建你的家庭药箱</view>
        <view class="family-info">
          <text>简单填写 3 项，开启家庭共享</text>
        </view>
      </view>
    </view>

    <view class="container">
      <view class="app-form-card">
        <view class="app-form-field">
          <view class="app-form-label">
            <text class="app-form-required">*</text>
            家庭名称
          </view>
          <input
            class="app-form-input"
            type="text"
            maxlength="20"
            placeholder="例如：我们家的药箱"
            value="{{form.familyName}}"
            bindinput="onFamilyNameInput"
          />
        </view>
        <view class="app-form-field">
          <view class="app-form-label">
            <text class="app-form-required">*</text>
            管理员姓名
          </view>
          <input
            class="app-form-input"
            type="text"
            maxlength="10"
            placeholder="请输入你的姓名或称呼"
            value="{{form.adminName}}"
            bindinput="onAdminNameInput"
          />
        </view>
        <view class="app-form-field">
          <view class="app-form-label">家庭备注</view>
          <textarea
            class="app-form-textarea"
            maxlength="80"
            placeholder="可选"
            value="{{form.note}}"
            bindinput="onNoteInput"
          />
          <view class="app-form-helper">比如：家里常用药，爸妈、我和妹妹共用。</view>
        </view>
        <view class="btn-create" bindtap="onSubmitCreate">创建家庭</view>
        <view class="app-form-helper">
          家庭创建后，你可以录入药品并邀请家人。家庭邀请码将在创建后生成。
        </view>
        <view class="cloud-sync-hint" bindtap="checkCloudSync">{{cloudChecking ? '正在检测云同步...' : cloudSyncText}} · 点击检测</view>
      </view>

      <view class="tip-box mt-32">
        <view class="tip-icon">💚</view>
        <view class="tip-text">
          <view class="tip-title">药无忧 · 小贴士</view>
          <view class="tip-desc">药无忧只做家庭药品库存、有效期、提醒和补货管理，不提供医疗诊断和用药建议。</view>
        </view>
      </view>
    </view>
  </block>

  <!-- ===== 有家庭阶段 ===== -->
  <block wx:else>
    <view class="family-header">
      <view class="family-bg"></view>
      <view class="family-inner">
        <view class="family-icon-wrap">
          <view class="family-icon">🏠</view>
        </view>
        <view class="family-name">{{family.name}}</view>
        <view class="family-info">
          <text>邀请码</text>
          <view class="family-code" bindtap="copyInviteCode">
            <text class="family-code-text">{{family.inviteCode}}</text>
            <text class="family-code-copy">复制</text>
          </view>
        </view>
      </view>
    </view>

    <view class="container">
      <view class="overview-card" style="margin-top: -40rpx;">
        <view class="overview-title">
          <text class="overview-title-icon">📋</text>
          家庭概览
        </view>
        <view class="overview-grid">
          <view class="overview-item">
            <view class="overview-num">{{overview.totalMemberCount}}</view>
            <view class="overview-label">成员总数</view>
          </view>
          <view class="overview-item">
            <view class="overview-num text-warn">{{overview.elderCount}}</view>
            <view class="overview-label">老人人数</view>
          </view>
          <view class="overview-item">
            <view class="overview-num">{{overview.medicineCount}}</view>
            <view class="overview-label">药品主档</view>
          </view>
          <view class="overview-item">
            <view class="overview-num">{{overview.batchCount}}</view>
            <view class="overview-label">总批次数</view>
          </view>
        </view>
        <view class="overview-footer">
          <view class="overview-info-row">
            <text class="overview-info-key">管理员</text>
            <text class="overview-info-val">👨‍💼 {{overview.adminName}}</text>
          </view>
          <view class="overview-info-row">
            <text class="overview-info-key">创建时间</text>
            <text class="overview-info-val">{{overview.createdText}}</text>
          </view>
          <view class="overview-info-row">
            <text class="overview-info-key">家庭邀请码</text>
            <text class="overview-info-val overview-code-val">{{family.inviteCode}}</text>
          </view>
        </view>
      </view>
      <view class="cloud-sync-hint cloud-sync-hint-card" bindtap="checkCloudSync">
        {{cloudChecking ? '正在检测云同步...' : cloudSyncText}} · 点击重新检测
      </view>

      <view class="invite-card" bindtap="openInvite">
        <view class="invite-icon">➕</view>
        <view class="invite-main">
          <view class="invite-title">邀请家人加入家庭</view>
          <view class="invite-desc">家人加入后可以查看家庭药品台账，老人默认只读，管理员可以录入和编辑药品。</view>
        </view>
        <view class="invite-arrow">›</view>
      </view>

      <view class="section-title mt-32">角色权限说明</view>
      <view class="role-grid">
        <view
          class="role-card"
          wx:for="{{rolePermissions}}"
          wx:key="key"
          style="background: {{item.bgColor}}; border-color: {{item.color}};"
        >
          <view class="role-card-head">
            <text class="role-card-icon">{{item.icon}}</text>
            <text class="role-card-label" style="color: {{item.color}};">{{item.roleLabel}}</text>
          </view>
          <view class="role-card-desc">{{item.desc}}</view>
          <view class="role-card-who">适合：{{item.who}}</view>
        </view>
      </view>

      <view class="section-title mt-32">家庭成员（{{members.length}}）</view>
      <view class="member-list">
        <view
          class="member-card"
          wx:for="{{members}}"
          wx:key="id"
          data-member="{{item}}"
          bindtap="onMemberTap"
        >
          <view class="member-avatar">{{item.avatar}}</view>
          <view class="member-main">
            <view class="member-header">
              <text class="member-name">{{item.name}}</text>
              <text
                class="tag"
                wx:if="{{item.role === 'admin'}}"
                style="background: #e7f2ee; color: #2e7d6a;"
              >管理员</text>
              <text
                class="tag"
                wx:if="{{item.role === 'elder'}}"
                style="background: #fff8e1; color: #f39c12;"
              >老人</text>
              <text
                class="tag"
                wx:if="{{item.role === 'member'}}"
                style="background: #e8f4fd; color: #3498db;"
              >家庭成员</text>
              <text
                class="tag tag-edit {{item.editable ? 'tag-edit-yes' : 'tag-edit-no'}}"
              >{{item.editableText}}</text>
            </view>
            <view class="member-sub">
              <text>与户主关系：{{item.relation}}</text>
              <text class="member-sub-dot">·</text>
              <text>加入：{{item.joinTime}}</text>
            </view>
            <view class="member-perm">权限：{{item.permissionSummary}}</view>
          </view>
          <view class="member-arrow">›</view>
        </view>
      </view>

      <view class="section-title mt-32">家庭信任与透明</view>
      <view class="quick-row">
        <view class="quick-mini" bindtap="goPrivacy">
          <view class="quick-mini-icon" style="background: #e8f4fd; color: #3498db;">🔒</view>
          <view class="quick-mini-main">
            <view class="quick-mini-title">隐私与授权</view>
            <view class="quick-mini-desc">查看采集字段与用户权利</view>
          </view>
          <view class="quick-mini-arrow">›</view>
        </view>
        <view wx:if="{{isAdmin}}" class="quick-mini" bindtap="goAccessLogs">
          <view class="quick-mini-icon" style="background: #f5eef9; color: #8e44ad;">📋</view>
          <view class="quick-mini-main">
            <view class="quick-mini-title">访问与操作记录</view>
            <view class="quick-mini-desc">追溯所有重要操作</view>
          </view>
          <view class="quick-mini-arrow">›</view>
        </view>
      </view>

      <view class="tip-box mt-32">
        <view class="tip-icon">💚</view>
        <view class="tip-text">
          <view class="tip-title">药无忧 · 小贴士</view>
          <view class="tip-desc">药无忧只做家庭药品库存、有效期、提醒和补货管理，不提供医疗诊断和用药建议。</view>
        </view>
      </view>
    </view>

    <!-- 邀请弹窗：分享 / 邀请码 -->
    <view class="mask {{showInvite ? 'show' : ''}}" bindtap="closeInvite"></view>
    <view class="sheet {{showInvite ? 'show' : ''}}" catchtap="">
      <view class="sheet-handle"></view>
      <view class="sheet-title">邀请家人加入家庭</view>
      <view class="sheet-desc">选择一种方式，将家人加入「{{family.name}}」</view>

      <button
        wx:if="{{cloudShareReady}}"
        class="invite-way invite-share-button {{cloudShareReady ? '' : 'invite-way-disabled'}}"
        open-type="share"
      >
        <view class="invite-way-icon" style="background: #e7f2ee; color: #2e7d6a;">📤</view>
        <view class="invite-way-main">
          <view class="invite-way-title">微信分享邀请</view>
          <view class="invite-way-desc">
            直接发送给家人的微信，打开后自动进入家庭
          </view>
        </view>
        <view class="invite-way-arrow">›</view>
      </button>
      <view wx:else class="invite-way invite-way-disabled" bindtap="shareInvite">
        <view class="invite-way-icon" style="background: #e7f2ee; color: #2e7d6a;">📤</view>
        <view class="invite-way-main">
          <view class="invite-way-title">微信分享邀请</view>
          <view class="invite-way-desc">请先确认云同步成功后再邀请家人</view>
        </view>
        <view class="invite-way-arrow">›</view>
      </view>

      <view class="invite-way" bindtap="copyInviteCode">
        <view class="invite-way-icon" style="background: #fff8e1; color: #f39c12;">🔢</view>
        <view class="invite-way-main">
          <view class="invite-way-title">邀请码邀请</view>
          <view class="invite-way-desc">
            邀请码：<text class="invite-code-big">{{family.inviteCode}}</text>
          </view>
        </view>
        <view class="invite-way-btn">复制</view>
      </view>

      <view wx:if="{{isAdmin}}" class="invite-way" bindtap="rotateInviteCode">
        <view class="invite-way-icon" style="background: #e8f4fd; color: #2980b9;">↻</view>
        <view class="invite-way-main">
          <view class="invite-way-title">重新生成邀请码</view>
          <view class="invite-way-desc">旧邀请码立即失效，新邀请码有效 30 天</view>
        </view>
        <view class="invite-way-arrow">›</view>
      </view>

      <view class="invite-way invite-way-elder" bindtap="openElderAgreement">
        <view class="invite-way-icon" style="background: #eafaf1; color: #27ae60;">👁</view>
        <view class="invite-way-main">
          <view class="invite-way-title">邀请长辈加入（大字版）</view>
          <view class="invite-way-desc">大字版授权说明，可先预览再让长辈阅读并加入</view>
        </view>
        <view class="invite-way-arrow">›</view>
      </view>

      <view class="sheet-close" bindtap="closeInvite">取消</view>
    </view>

    <!-- 老人加入预览（大字版授权说明） -->
    <view class="mask {{showElderAgreement ? 'show' : ''}}" bindtap="closeElderAgreement"></view>
    <view class="elder-agree {{showElderAgreement ? 'show' : ''}}" catchtap="">
      <view class="elder-agree-head">
        <text class="elder-agree-emoji">🧓</text>
        <view class="elder-agree-title">邀请您加入家庭药箱</view>
      </view>

      <view class="elder-agree-card">
        <view class="elder-agree-row elder-agree-highlight">
          <text class="elder-agree-row-key">👦 谁邀请我</text>
          <text class="elder-agree-row-val">{{elderAgreementContent.inviterName}}（您的{{elderAgreementContent.inviterRelation}}）</text>
        </view>
        <view class="elder-agree-row elder-agree-highlight">
          <text class="elder-agree-row-key">🏠 加入哪个家庭</text>
          <text class="elder-agree-row-val">{{elderAgreementContent.familyName}}</text>
        </view>
        <view class="elder-agree-divider"></view>
        <view class="elder-agree-row">
          <text class="elder-agree-row-key">👀 谁能看到我的提醒</text>
          <text class="elder-agree-row-val">家庭管理员 {{elderAgreementContent.inviterName}} 和您信任的家人</text>
        </view>
        <view class="elder-agree-row">
          <text class="elder-agree-row-key">💊 他们会看到什么</text>
          <text class="elder-agree-row-val">药品台账、服药确认状态、临期/过期/库存不足提醒</text>
        </view>
        <view class="elder-agree-row">
          <text class="elder-agree-row-key">✅ 我能做什么</text>
          <text class="elder-agree-row-val">查看今日提醒、点击「已服用」、一键联系家人</text>
        </view>
        <view class="elder-agree-row">
          <text class="elder-agree-row-key">🚫 我不能做什么</text>
          <text class="elder-agree-row-val">不能编辑、删除或新增药品（由孩子代为管理）</text>
        </view>
        <view class="elder-agree-row">
          <text class="elder-agree-row-key">🚪 如何退出</text>
          <text class="elder-agree-row-val">可在「我的」页面退出家庭，退出后无法再查看。</text>
        </view>
      </view>

      <view class="elder-agree-tip">
        <text class="elder-agree-tip-icon">💚</text>
        <text class="elder-agree-tip-text">
          药无忧只做家庭药品库存、有效期、提醒和补货管理，不提供用药建议，具体请遵医嘱。
        </text>
      </view>

      <view class="elder-agree-btns">
        <view class="elder-agree-btn elder-agree-btn-primary" bindtap="onAgreeJoin">我已了解并同意加入</view>
        <view class="elder-agree-btn elder-agree-btn-default" bindtap="closeElderAgreement">暂不加入</view>
      </view>
    </view>

  </block>
</view>
