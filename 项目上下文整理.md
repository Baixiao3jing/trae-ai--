# 药无忧项目上下文整理

## 1. 项目定位

项目名称：药无忧

产品形态：微信小程序

核心定位：面向年轻子女的家庭药品智能管理小程序。年轻人/子女作为家庭管理员，创建家庭空间、邀请父母加入，负责药品录入、分类、批次库存和提醒管理。老人端只负责查看大字提醒、查看药品信息、点击“已服用”。

产品边界：

- 不判断药品疗效。
- 不推荐用药。
- 不替代医生诊疗建议。
- 只做家庭药品库存、有效期、批次、提醒和补货管理。

核心场景：

1. 用户自己创建一个家庭药箱。
2. 用户录入药品。
3. 用户邀请家人加入家庭。
4. 同一种药支持多个批次、不同有效期。
5. 老人确认服药后，子女端能看到状态变化。

## 2. 当前代码路径

程序路径：

```text
D:\桌面\trae ai大赛\程序
```

重要文件：

```text
app.json
app.wxss
utils/appStore.js
utils/mockData.js
utils/demoStore.js
pages/index/index
pages/medicines/medicines
pages/add-medicine/add-medicine
pages/medicine-detail/medicine-detail
pages/family/family
pages/elder/elder
pages/settings/settings
pages/privacy/privacy
pages/access-logs/access-logs
```

底部 tabBar 目前应只保留 3 个：

- 首页
- 药箱
- 我的

## 3. 已完成阶段

### 第一阶段：基础框架

已完成：

- 原生微信小程序基础结构。
- 首页、药箱、我的 3 个 tab。
- 家庭管理、添加药品、药品详情、老人端提醒等非 tab 页面。
- 基础页面跳转。

### 第二阶段：正式数据模型

已完成：

- `family`
- `members`
- `medicines`
- `medicineBatches`
- `medicationPlans`
- `medicationRecords`

核心数据模型已经从简单展示数据，升级为：

```text
家庭空间 + 家庭成员 + 药品主档 + 批次库存 + 用药计划 + 服药记录
```

已实现的计算能力：

- 药品汇总
- 首页统计
- 今日关注
- 药品批次排序
- 药箱筛选

### 第三阶段：条码 + OCR + 人工确认流程

已完成演示版流程：

- 选择录入方式
- 条码模拟识别
- OCR 模拟识别
- 人工确认
- 保存成功

后来发现问题：这个流程过于演示化，不适合作为真实产品流程。

### 第四阶段：老人端确认同步

已完成：

- `demoStore.js` 本地演示状态。
- 老人端点击“已服用”后，首页待确认数量可同步变化。
- 我的页支持角色切换。
- 支持重置演示数据。

### 第五阶段：家庭邀请与隐私可信度

已规划/部分完成方向：

- 家庭邀请。
- 老人加入授权说明。
- 隐私与授权页面。
- 访问记录页面。
- 数据导出演示。

### 第六阶段：真实用户流程

目标方向已经明确：

从“预填演示数据的小程序”转成“真实用户从零开始使用的小程序”。

核心变化：

- 不默认创建“爸妈家的药箱”。
- 不默认展示小李、爸爸、妈妈。
- 不默认展示药品、批次、提醒。
- 用户首次进入时，需要自己创建家庭。
- 创建家庭后，药箱初始为空。
- 用户自己录入药品。
- 用户自己邀请家人。

`utils/appStore.js` 已经出现，说明项目正在向真实本地状态流转：

- `currentUser`
- `currentFamily`
- `members`
- `medicines`
- `medicineBatches`
- `medicationPlans`
- `medicationRecords`
- `accessLogs`

## 4. 当前最大问题：添加药品流程仍有演示痕迹

用户指出的问题是正确的。

当前添加药品页面仍存在：

1. 扫条码录入没有调用摄像头。
2. 页面里展示“模拟扫码区域”。
3. 用户需要从预设药品里选择条码。
4. 拍药盒识别没有拍照或相册入口。
5. OCR 页面展示“高置信度 / 低置信度 / 识别失败”模拟选项。
6. 选择页底部提示过多，不像正常产品。

真实产品应改成：

### 扫条码录入

用户点击“扫条码录入”后，应调用：

```js
wx.scanCode({
  onlyFromCamera: false,
  scanType: ['barCode', 'qrCode']
})
```

流程：

1. 打开扫码界面。
2. 获取扫码结果。
3. 用扫码结果匹配内部条码库或本地已有药品。
4. 命中则展示识别结果。
5. 未命中则进入手动填写。

不能再让用户选择预设条码。

### 拍药盒识别

用户点击“拍药盒识别”后，应提供：

- 拍照
- 从相册选择

优先使用：

```js
wx.chooseMedia({
  count: 1,
  mediaType: ['image'],
  sourceType: ['camera', 'album']
})
```

兼容兜底：

```js
wx.chooseImage({
  count: 1,
  sourceType: ['camera', 'album']
})
```

流程：

1. 用户拍照或选择图片。
2. 页面展示图片预览。
3. 用户点击“开始识别”。
4. 当前没有真实 OCR 时，可以内部使用 mock OCR 结果。
5. 但界面上不能让用户选择模拟结果。

### 手动录入

点击手动录入后，应进入空白确认表单。

不应该默认填入：

- 苯磺酸氨氯地平片
- 默认批号
- 默认有效期
- 默认厂家

可以保留默认单位“片”。

## 5. 最近已修复的问题

手机预览时报错：

```text
预览 Error: 220:0:unexpected character `\n`
File: pages/medicine-detail/medicine-detail.wxml
```

原因：

`medicine-detail.wxml` 中进度条 `style` 属性被拆成多行，并包含三元表达式。手机预览 WXML 编译器比开发工具本地更严格。

修复方式：

- 在 `medicine-detail.js` 中提前计算 `progressStyle`。
- 在 WXML 中改为：

```html
style="{{item.progressStyle}}"
```

## 6. 下一步推荐提示词方向

下一步应让 Tree/Trae 修改“添加药品”流程，把它从演示版改成真实产品流程。

核心要求：

- 删除“模拟扫码区域”。
- 删除预设条码列表。
- 删除“模拟 OCR 识别”。
- 删除“比赛演示版”文案。
- 扫条码必须调用 `wx.scanCode`。
- 拍药盒必须调用 `wx.chooseMedia` 或 `wx.chooseImage`。
- OCR mock 可以保留在内部，但不能暴露给用户选择。
- 手动录入表单应为空。
- 保存后继续写入 `appStore` 本地 storage。

## 7. 给 Tree/Trae 的下一步提示词

```text
请修改当前“药无忧”小程序的添加药品流程，把它从“演示版模拟选择流程”改成更接近真实产品的录入流程。

重要要求：
1. 不要重做项目结构。
2. 不要修改底部 tabBar。
3. 不要破坏已有功能：
   - 创建家庭
   - 药箱列表
   - 药品详情
   - 本地 storage 写入
   - 同名/同条码药品新增批次
4. 本阶段只改 pages/add-medicine/add-medicine 相关流程，以及必要的 app.json 权限配置。
5. 可以继续使用 mockBarcodeLibrary / mockOcrResults 作为“内部兜底识别结果”，但不要在界面上让用户选择模拟条码或模拟 OCR 场景。

一、选择录入方式页调整

当前 choose 页面有三种方式：
- 扫条码录入
- 拍药盒识别
- 手动录入

这三个入口保留，但页面要去掉明显演示化内容。

请删除或弱化以下内容：
1. 删除底部“关于 AI 识别说明”大卡片。
2. 删除“模拟”“比赛演示版”“请选择一个模拟条码”“请选择一种识别结果”等字样。
3. 删除过多的对勾提示，比如“减少手动输入”“基础信息更准确”“显示置信度”等。
4. 可以保留一句简短说明：
   “录入后请核对药品名称、有效期和数量，确认后才会保存。”

二、扫条码录入改成真实扫码

请修改 onScanBarcode 逻辑：

1. 用户点击“扫条码录入”后，直接调用：

wx.scanCode({
  onlyFromCamera: false,
  scanType: ['barCode', 'qrCode']
})

2. 扫码成功后：
   - 获取 result。
   - 将 result 作为 barcode。
   - 调用现有 lookupBarcode(barcode)。
   - 如果 lookupBarcode 命中，自动填充药品基础信息，并进入条码识别结果步骤。
   - 如果 lookupBarcode 未命中，展示扫到的条码，并提供“手动填写药品信息”和“重新扫码”。

3. 不要再展示预设条码列表。
4. 不要再让用户选择预设药品。
5. 预设条码库只作为内部匹配库使用。

三、拍药盒识别改成真实拍照/选择图片入口

用户点击“拍药盒识别”后，提供：
- 拍照
- 从相册选择

优先使用 wx.chooseMedia：

wx.chooseMedia({
  count: 1,
  mediaType: ['image'],
  sourceType: ['camera', 'album']
})

如果不兼容，则使用 wx.chooseImage 兜底。

用户选择图片后：
1. 保存 imageTempPath。
2. 展示图片预览。
3. 提示：“请确认图片中能看到生产日期、有效期或批号。”
4. 提供按钮：
   - 开始识别
   - 重新选择图片

四、OCR 识别流程调整

没有真实 OCR 接口时，可以继续使用 mockOcrResults，但不要让用户选择高置信度/低置信度/失败。

点击“开始识别”后：
1. 显示 loading：识别中...
2. 默认返回一组高置信度 OCR 结果。
3. 识别完成后展示“批次识别结果”。

批次识别结果展示：
- 图片预览
- 批号
- 生产日期
- 有效期
- 总数量
- 单位
- 识别置信度
- 日期来源标注

按钮：
- 进入人工确认
- 重新拍照
- 手动填写

五、手动录入调整

点击“手动录入”后：
1. 直接进入人工确认页。
2. 表单应尽量为空。
3. 不要默认填入任何具体药品。
4. 可以保留默认单位“片”。

六、人工确认页保留

确认页继续保留：
1. 所有字段都允许修改。
2. 保存前校验必填字段。
3. 保存逻辑保持：
   - 同条码或同名药品已存在：新增批次
   - 不存在：创建药品主档 + 第一个批次
4. 保存后真实写入本地 storage。

七、权限配置

如有需要，在 app.json 中补充摄像头权限说明：

"permission": {
  "scope.camera": {
    "desc": "用于扫描药盒条码和拍摄药盒有效期信息"
  }
}

以微信开发者工具可编译为准。

八、验收路径

路径 1：扫条码
1. 点击添加药品。
2. 点击扫条码录入。
3. 手机打开扫码界面。
4. 扫码成功后展示条码识别结果。
5. 点击下一步进入拍药盒补充有效期。
6. 选择图片或拍照。
7. 点击开始识别。
8. 进入人工确认。
9. 保存成功后药箱出现该药品。

路径 2：拍药盒
1. 点击添加药品。
2. 点击拍药盒识别。
3. 弹出拍照/相册选择。
4. 用户选择图片。
5. 页面展示图片预览。
6. 点击开始识别。
7. 展示 OCR 识别结果。
8. 进入人工确认。
9. 保存成功。

路径 3：手动录入
1. 点击添加药品。
2. 点击手动录入。
3. 进入空白人工确认表单。
4. 用户填写字段。
5. 保存成功。
```

## 8. 当前原则

后续分支目标：

```text
保留框架，去掉预填演示，把它改成用户自己一步步创建出来的真实产品。
```

可以内部保留 mock 能力，但不能让普通用户在页面上看到“模拟选择器”。

